# Next.js Player Starter

Next.js 16 starter for embedding the Streamlet React player.

## Setup

```bash
npm install
cp .env.example .env.local
npm run dev
```

Add a Streamlet playback URL to `.env.local` or paste one into the form in the app.

